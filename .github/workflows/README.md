# CI

`verify.yml` re-executes every corpus label and runs the scorer's tests on each push.

It deliberately does **not** run the scanners. Their results depend on versions, network
access and paid credentials, so a leaderboard regenerated by CI would drift silently and
without a reproducible provenance. Results are generated deliberately, on a known machine,
with versions recorded in `harness/results-latest.json`.
