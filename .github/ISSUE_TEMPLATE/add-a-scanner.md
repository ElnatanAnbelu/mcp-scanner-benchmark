---
name: Add a scanner
about: Propose a scanner this benchmark should cover
title: "[scanner] "
labels: scanner
---

**Tool and repository**

**How it installs**
Ideally a single command. Note if it needs an account, an API key, or paid credentials —
availability is recorded as a result, not a blocker.

**What surface does it inspect**
`metadata` (reads a live server), `source` (reads code), or `runtime` (drives and exploits).

**What languages does it parse**

**Machine-readable output?**
JSON or SARIF, and the flag that produces it.
